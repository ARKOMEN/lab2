#include "parser.h"

parser::parser(std::string txt, std::vector<std::string> input_files, std::string output_file):_txt(txt), _input_files(input_files), _output_file(output_file){
    //проверить на ошибочный ввод
}

parser::~parser() {
    //может быть нужен будет деструктор
}